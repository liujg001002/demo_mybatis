package com.mybatis.test;

import org.apache.shiro.crypto.hash.SimpleHash;
import org.apache.shiro.util.ByteSource;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest
public class DemoMybatisApplicationTests {

	@Test
	public void contextLoads() {


//		String hashAlgorithmName = "MD5";//加密方式
//		String credentials = "111111";//加密次数
//		int hashIterations = 1;//加密次数
//		ByteSource salt = ByteSource.Util.bytes("1");//盐值
//		Object obj = new SimpleHash(hashAlgorithmName, credentials, salt, hashIterations);
//		System.out.println(obj);
//		System.out.println(obj.toString().length());

	}

}
